import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  individualCards: {
    backgroundColor: "white",
  },
  description: {
    overflowY: "scroll",
    maxHeight: "150px",
  },
}));
