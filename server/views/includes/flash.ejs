<% errors.forEach(function(message){ %>
    <div>
        <%= message %>
    </div>
    <% }) %>
        <% success.forEach(function(message){ %>
            <div>
                <%= message %>
            </div>
            <% }) %>